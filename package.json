{
  "name": "thresholds",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "dependencies": {
    "lerna": "3.22.1",
    "typescript": "4.0.2",
    "wsrun": "5.2.1"
  },
  "scripts": {
    "prebuild": "yarn workspaces foreach run prebuild",
    "start": "yarn workspaces foreach -p -i -v -j 20 run start",
    "build": "yarn workspaces foreach -pt -i -v run build"
  }
}
